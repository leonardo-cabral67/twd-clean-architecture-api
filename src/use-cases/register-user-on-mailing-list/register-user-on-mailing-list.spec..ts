import { UserData } from "./user-data";

describe("Register user on mailing list", () => {
  test("should add user with complete data to mailing list", async () => {
    const users: UserData[] = [];
    console.log(users)
    // const repo: UerRepository = new InMemoryUserRepository(users);
    // const useCase = new RegisterUserOnMailingList(repo);
    // const name = "Victoria";
    // const email = "Victoria@email.com";
    // const response = await useCase.registerUserOnMailingList({ name, email });
    // const user = repo.findUserByEmail(email);
    // expect((await user).name).toBe(name);
  });
});
